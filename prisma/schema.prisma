generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model art_app_cloud {
  id                Int      @id @default(autoincrement())
  art_name          String
  description       String
  owner             String
  pic               String

}
